I will transform the project to **On-Page Delivery Mode** as per your specific instructions.

### **1. Core Logic Changes**
*   **Checkout**: Redirect users to `order-status.html` instead of waiting for an email.
*   **Delivery**: Frontend polls for the license key; Backend waits for IPN or Admin fulfillment.
*   **Fulfillment**: IPN only marks "PAID"; actual key delivery happens via Admin API (manual or script).

### **2. Frontend Implementation (`public/`)**
*   **New `public/order-status.html`**:
    *   **Logic**: Polls `/api/check-order-status` every 3 seconds.
    *   **UI**: Shows a loading spinner while processing. Once `completed`, displays the License Key with a "Copy" button.
*   **Update `public/buy.html`**:
    *   Change form submission to `fetch('/api/checkout')`.
    *   Redirect to the returned `payment_url`.
*   **New `public/query.html`**:
    *   Simple input for Email to find the last order and retrieve the key (fallback mechanism).

### **3. Backend Implementation (`api/`)**
*   **New `/api/check-order-status`**:
    *   Checks Supabase for `order_id`.
    *   Returns `completed` (with key), `processing` (paid but no key), or `pending`.
*   **Update `/api/checkout.js`**:
    *   Set `success_url` to `${process.env.SITE_URL}/order-status.html?order_id=...`.
*   **Update `/api/nowpayments-ipn.js`**:
    *   **Remove** all email sending logic.
    *   Only update `np_status` in the database.
*   **New `/api/admin/fulfill.js`**:
    *   Accepts `{ order_id, license_key, admin_secret }`.
    *   Updates the order with the key and sets `is_delivered = true`.
*   **New `/api/query-last-order.js`**:
    *   Finds the most recent completed order for a given email.

### **4. Configuration**
*   No database schema changes needed (fields `license_key` and `is_delivered` already exist).
*   Requires `ADMIN_SECRET` and `SITE_URL` environment variables.

I am ready to implement these changes immediately.