# GitHub local setup

## Preferred tool
- GitHub CLI: `gh`

## Checks
- `gh --version`
- `gh auth status`

## If missing
Install GitHub CLI or use browser fallback.

## If unauthenticated
Use `gh auth login` or browser-based sign-in.
