# Gemini CLI local setup

## Package
- `@google/gemini-cli`

## Checks
- `gemini --version`
- auth/login state if required by the installed flow

## Local machine note
Install into the workspace toolchain or globally if needed.
