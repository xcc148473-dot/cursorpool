# Template index

- `templates/launch-app.ahk` — launch an app and wait for its window
- `templates/focus-window-and-send.ahk` — activate a window and send a shortcut
- `templates/file-dialog-open.ahk` — enter a path into an Open dialog
- `templates/file-dialog-save.ahk` — enter a path into a Save dialog

Adapt these templates instead of starting from scratch.
