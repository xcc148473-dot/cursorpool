---
name: provider-sync-local
description: Review and sync provider/model configuration for OpenClaw installations. Use when adding providers, reconciling model lists, or updating provider settings safely.
---

# Provider Sync Local

## Workflow
1. Inspect current config.
2. Compare desired providers/models.
3. Apply minimal safe changes.
4. Verify agent startup and model availability.
